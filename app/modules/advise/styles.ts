import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  card: {
    borderRadius: 10,
    marginTop: 10,
  },

  product: {
    paddingBottom: 10
  }
});

export default styles;